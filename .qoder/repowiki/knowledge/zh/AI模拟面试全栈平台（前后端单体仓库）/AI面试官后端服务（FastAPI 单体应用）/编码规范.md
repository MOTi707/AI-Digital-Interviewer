- 每个业务域按 routers/schemas/services/models 四层拆分，routers 只负责参数解析与调用 services，services 封装 SQL 查询与外部调用。
- 配置全部走 `app.config.get_settings()` 单例，禁止在各子模块内硬编码环境变量或连接串。
- 数据库访问统一使用 `app.database.async_session_factory` 提供的异步会话，不在路由层直接创建引擎。
- 对外请求体/响应体一律使用 Pydantic schema（`schemas/` 下同名文件）描述，避免裸 dict 透传。