TTS 使用 Microsoft Edge TTS 客户端库 `edge-tts`（Neural 语音 zh-CN-XiaoxiaoNeural）；代码执行器通过 `subprocess` 直接调用系统 gcc/g++/javac/node/python，配合 `ThreadPoolExecutor` 将阻塞 I/O 放入线程池避免阻塞事件循环；Pydantic v2 模型使用 `from_attributes = True` 替代已废弃的 `orm_mode`。