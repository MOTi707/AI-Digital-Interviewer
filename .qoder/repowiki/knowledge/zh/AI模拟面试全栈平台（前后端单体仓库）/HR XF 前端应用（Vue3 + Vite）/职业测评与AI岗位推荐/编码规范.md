- 所有异步请求统一在 try/catch/finally 中设置 `loading.value = true/false` 与 `error.value`，失败时把 `Error.message` 写入 error 供模板显示。
- UI 风格遵循 Memphis Neo-brutalism：按钮/卡片一律使用 `border-4 border-black shadow-[5px_5px_0px_0px_rgba(0,0,0,1)] hover:shadow-[2px_2px_0px_0px] hover:translate-x-[3px] hover:translate-y-[3px]` 组合。
- 三种测评类型通过字符串字面量 `'holland' | 'mbti' | 'career_values'` 驱动，各视图内用 `Record<string, ...>` 或 `if (type === ...)` 分支做标签/图标/颜色映射，避免硬编码枚举。