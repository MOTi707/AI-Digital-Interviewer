- 所有后端交互集中在 `stores/interview.ts` 的 `apiRequest<T>` 中，统一拼接 `API_BASE`、注入 `auth_token` 并从 `res.json()` 反序列化为泛型类型，视图侧不直接写 `fetch`。
- UI 弹窗一律通过自定义 `confirmModal`/`alertModal` 配合 `<Teleport to="body">` + `Transition name="modal-fade"` 实现，禁止使用浏览器原生 `alert`/`confirm`。
- 按钮与卡片统一使用「粗黑边框 + 硬阴影 + hover 位移缩小阴影」的 Neo-brutalism 风格（`border-4 border-black shadow-[Xpx_Ypx_0px_0px_rgba(0,0,0,1)] hover:translate-x-[N] hover:shadow-[Mpx_Mpx_...]`），颜色限定在 `#ff006e`/`#3a86ff`/`#ffbe0b`/`#22c55e`/`#8338ec` 五色板。
- 轮次枚举值（`assessment`/`tech`/`business`/`ai_voice_3`/`ai_voice_4`）在各处均以 `Record<string,string>` 映射表形式重复声明用于中文标签展示，保持前后端 key 一致。
- 异步方法遵循 `loading.value = true` → try/catch → `finally { loading.value = false }` 三件套模式，错误信息统一包装为 `Error` 并通过 `alert` 或 `console.error` 上报。