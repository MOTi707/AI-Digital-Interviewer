Vue 3 `<script setup>` + TypeScript + Pinia 状态管理；样式使用 Tailwind CSS 原子类；图表通过 `vue-echarts` 按需注册 `RadarChart`；AI 对话使用原生 `fetch` + `TextDecoder` 解析 `data:` 行实现的 SSE 流式输出；摄像头录制使用 `MediaRecorder`（优先 `video/webm;codecs=vp9`，回退 `video/webm`）。