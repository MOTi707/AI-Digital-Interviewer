- 路由通过 `meta.requiresAuth` / `meta.requiresAdmin` 标记权限，由 router 全局守卫统一拦截跳转。
- 每个业务域拥有独立的 Pinia store 文件（auth/resume/interview/career/oj/forum/admin），组件通过 `useXxxStore()` 访问状态而非 props 透传。
- 视图组件采用懒加载 `() => import('@/views/...')` 形式注册到路由，减少首屏体积。