根目录作为前后端双工程的聚合仓库：backEnd 提供 FastAPI 单体服务，集中挂载认证、论坛、OJ、测评、简历、面试、TTS 与管理路由；frontEnd 基于 Vue3 + Vite 构建 SPA，通过 HTTP 调用后端接口并共享同一套业务模型（用户、帖子、题目、面试记录等）。两端通过 .env / requirements.txt / package.json 各自管理依赖，由 Alembic 统一驱动数据库迁移，使前后端在数据契约层面保持同步。